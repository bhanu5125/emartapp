# Copy to kkartchart/values.secrets.yaml (gitignored) and fill in real values.
# Deploy with:
#   helm upgrade kubekart kkartchart --install -f kkartchart/values.secrets.yaml --namespace kart
kkartcharts-database:
  config:
    mysqlPassword: "changeme"

kkartcharts-backend:
  jwtSecret: "changeme"
